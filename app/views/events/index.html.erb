<h1>All Events</h1>
<%= link_to "Create Event!", new_event_path %>
<p>
	
</p>
<%= will_paginate %> 
<ul class = "users">
	<table summary="Index listing of events">
	<%= render @events %>
	</table>
</ul>
<%= will_paginate %> 
